# Optional. Only needed if you want the contact form to submit to a real
# service instead of falling back to a mailto: link. See README.md's
# "Contact form" section for a free option (Formspree) and setup steps.
#
# Copy this file to .env and fill in your own endpoint — never commit .env.
VITE_FORM_ENDPOINT=
